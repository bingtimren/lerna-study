{
  "name": "e2etest",
  "version": "0.1.0",
  "description": "End-to-end tests for all tools here",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "jest --runInBand --no-cache",
    "prepare": "rm -fr testProj/.git;git stash -- testProj;git restore --source=HEAD --worktree -- testProj;git clean -fdx testProj; cd testProj;git init;git add .;git commit -m init;git tag init;yarn install;cp -r ../testProjChange/* .",
    "clean": "cd testProj/.git;git stash -- .;git reset --hard;git clean -fd"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "child-process-toolbox": "^3.0.1",
    "execa": "^5.1.1",
    "jest": "^27.0.4",
    "ts-jest": "^27.0.3",
    "typescript": "^4.3.2"
  }
}
