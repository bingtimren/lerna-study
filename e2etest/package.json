{
  "name": "e2etest",
  "version": "0.1.0",
  "description": "End-to-end tests for all tools here",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "jest",
    "test-prepare": "yarn test-clean; cd testProj; git init; git add .;git commit -m init; yarn install",
    "test-clean": "rm -fr testProj/.git;git stash testProj;git reset --hard testProj;git clean -fdx testProj"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "child-process-toolbox": "^3.0.1",
    "execa": "^5.1.1",
    "jest": "^27.0.4",
    "ts-jest": "^27.0.3",
    "typescript": "^4.3.2"
  }
}
